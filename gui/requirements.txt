streamlit>=1.32
torch>=1.13
transformers>=4.30
openai>=1.0
python-dotenv>=1.0
# EHR section detection (optional — the app falls back to a built-in regex
# sectionizer if medspacy is not installed)
medspacy>=1.0
